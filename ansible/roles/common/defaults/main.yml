---
common_packages:
  - python3-pip
  - curl
  - git
  - vim
  - htop
  - ca-certificates
  - gnupg
  - lsb-release

common_timezone: "UTC"

# Users managed by the common role. Keep empty to skip user creation.
# Example:
# common_managed_users:
#   - name: deploy
#     groups: [sudo, docker]
#     shell: /bin/bash
common_managed_users: []
