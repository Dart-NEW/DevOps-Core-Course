apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-info-service-go.fullname" . }}-test-connection"
  labels:
    {{- include "devops-info-service-go.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-info-service-go.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
