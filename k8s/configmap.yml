---
# Deploy to default namespace or devops-lab
# kubectl apply -f k8s/

apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-config
  namespace: default
data:
  VERSION: "1.0"
  ENVIRONMENT: "kubernetes-lab"
